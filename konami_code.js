const code = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65]
var attempt = []

function init() {
  // Write your JavaScript code inside the init() function
}

function onKeyDownHandler(e){
  for(let i=0;i<=code.length;i++){
    if(attempt===code){
      alert("Welcome, Lain.")
      attempt.length=0
    }
    if(the input or we === code[i]){
      attempt.push(the input or whatever)
    }

    else{
      attempt.length=0
      i=0
    }
  }
}